version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Build
          command: ./gradlew build

workflows:
  build:
    jobs:
      - build
